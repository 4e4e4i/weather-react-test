import React from 'react';

import './spinner.scss'

const Spinner = () => {
    return (
        <div className="spinner">
            <span>Spinner!</span>
        </div>
    )
};

export default Spinner;
